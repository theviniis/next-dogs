import { Feed } from "@/components/feed/feed";

export default function Home() {
  return (
    <main className="pt-[var(--header-height)] min-h-dvh container my-5">
      <Feed />
    </main>
  );
}
